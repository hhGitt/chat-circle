export interface userImage {
    faceData: any
}

export interface userInfoParam {
    username: string;
    sex: number;
    signature: string;
    birthday: string;
    hobbies: string;
    regionId: string;
}

export interface chatUserInfo{
    userId: string
}